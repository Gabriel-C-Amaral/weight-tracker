# Create a Vercel KV store at vercel.com/dashboard → Storage → Create KV
# Then link it to your project — these vars are added automatically.
KV_URL=
KV_REST_API_URL=
KV_REST_API_TOKEN=
KV_REST_API_READ_ONLY_TOKEN=
